name: Testing IoT-Dashboard setup

on: [push]

jobs:
  run-github-actions:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@docker-ttn-dashboard-v1.0
      - name: List Files
        run: |
          hostname -f
          pwd
          ls -a
